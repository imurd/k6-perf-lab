// ============================================================
//  Общие настройки для всех сценариев.
//  Держим их в одном месте, чтобы не дублировать по файлам.
// ============================================================

// Учебный REST-API от создателей k6 — можно свободно нагружать.
// Позже заменишь на адрес своего/рабочего сервиса.
export const BASE_URL = "https://test-api.k6.io";

// --- Целевые показатели (SLO — Service Level Objectives) ---
// Это ТВОЙ контракт: "быстрее чего и с какой долей ошибок считаем нормой".
// Пороги ниже читаются так:
//   http_req_duration p(95)<500  — 95% запросов должны укладываться в 500 мс
//   http_req_failed   rate<0.01  — доля упавших запросов меньше 1%
// Если реальность хуже порога — k6 завершится с ошибкой (exit code 1).
// Это и превращает нагрузочный тест в автоматическую проверку (для CI).
export const THRESHOLDS = {
  http_req_duration: ["p(95)<500", "p(99)<1000"],
  http_req_failed: ["rate<0.01"],
};
